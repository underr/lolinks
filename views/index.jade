include header

body
 .container
  #add
   h3 ♥ lolinks ♥

    form(action="add", method="post")
     label Título
     input(type="text", name="title", placeholder="Título", size="20", autofocus="autofocus")
     label URL
     input(type="text", name="url", placeholder="URL", size="34")
     br
     label Descrição (max 70 caracteres)
     input(type="text", name="dcr", placeholder="Descrição (max 70 caracteres)", size="60", heigth="5")
     br
     input(type="submit", value="Enviar~")
  .container.one-half.column 
   - for(bookmark in bookmarks) {
    a(href=bookmarks[bookmark].url, title=bookmarks[bookmark].id)= bookmarks[bookmark].title
    |  · #{bookmarks[bookmark].date}
    p#dcr #{bookmarks[bookmark].dcr}
   - }

  br
  #pager
     each val in tpages
      a(href= val, class='pag')
        = val
